# 공통
numpy
pypdfium2          # PDF -> 이미지. poppler 같은 시스템 패키지가 필요 없다
Pillow

# tesseract (엔진 본체는 시스템 패키지로 따로 깔아야 한다)
#   Ubuntu: sudo apt-get install -y tesseract-ocr tesseract-ocr-kor tesseract-ocr-eng
#   Windows: https://github.com/UB-Mannheim/tesseract/wiki  설치 후 --tesseract-cmd 로 경로 지정
pytesseract

# easyocr (torch 를 끌고 온다. GPU 서버에서는 CUDA 빌드 torch 를 먼저 깔아라)
easyocr

# PaddleOCR-VL 1.6 — 아래 두 줄은 CUDA 버전을 타므로 requirements 로 안 걸고 직접 깐다.
#   python -m pip install paddlepaddle-gpu==3.2.1 -i https://www.paddlepaddle.org.cn/packages/stable/cu126/
#   python -m pip install -U "paddleocr[doc-parser]>=3.6.0"

# 결과 대시보드 (2-2_document_ocr.py) — 로컬에서만 쓴다. GPU 서버에는 깔지 않는다
#   OCR_DIR=result streamlit run 2-2_document_ocr.py
streamlit
altair
